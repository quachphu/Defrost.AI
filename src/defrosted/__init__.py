"""Defrosted.ai — AI rental agent.

This package is intentionally import-light at the top level: importing
``defrosted`` must NOT pull in heavy runtime deps (Temporal, Playwright,
LangGraph). Import those from their specific submodules instead.
"""

__version__ = "0.1.0"
